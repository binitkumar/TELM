<!--div id="content-header">
<%= image_tag("/images/show_timetable.png") %>
  <h1><%#= t('timetable_text') %>Study Material</h1>
  <h3><%#= t('select_class_to_edit') %>Manage study materials</h3>
  <div id="app-back-button">
    <%= link_to_function image_tag("/images/buttons/back.png",:border => 0), "history.back()" %>
  </div>
</div>

<div id="page-yield">

  <% unless flash[:notice].nil? %>
    <p class="flash-msg"> <%= flash[:notice] %> </p>
  <% end %>

  <% form_for :timetable_entry do |s| %>

    <div class="label-field-pair">
      <label for="timetable_entry_course_id"><%= t('select_a_batch') %></label>
      <div class="text-input-bg"> <%= select :study_material, :batch_id, @batches.map {|c| [c.full_name, c.id]},:prompt=>"#{t('select_a_batch')}" %>
      </div>
    </div>

    <div class="submit-button">
      <%= submit_tag " #{t('go')}", :class=>'submit-button' %>
    </div>
    
  <% end %>

</div-->

<div id="content-header">
 <%= image_tag("/images/student_details/student_details_logo.jpg") %>
  <h1><%= t('student_details') %> </h1>
  <h3><%= t('view_details') %> </h3>
  <div id="app-back-button">
    <%= link_to_function image_tag("/images/buttons/back.png",:border => 0), "history.back()" %>
  </div>
</div>

<div id="page-yield">

  <% unless flash[:notice].nil? %>
    <p class="flash-msg"> <%= flash[:notice] %> </p>
  <% end %>

  <div class="label-field-pair-select-class">
    <label for="student-detail-select-class"><%= t('select_a_batch') %> </label>
    <div class="text-input-bg"><%= select :course, :course_id, 
        @batches.map {|b| [b.full_name, b.id]},
        {:prompt => "#{t('select_a_batch')}"},
        {:onChange => "#{remote_function(:url => {:action => "list_uploads_by_course"},
        :with => "'batch_id='+value",
        :before => "Element.show('loader')",
        :success => "Element.hide('loader')"
        )}"} %></div>
    <%= image_tag("loader.gif",
      :align => "absmiddle",
      :border => 0,
      :id => "loader",
      :style =>"display: none;" ) %>
  </div>
  <div id="students"> </div>

</div>